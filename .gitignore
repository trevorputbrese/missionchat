
/buildplan/
/target/
/.idea/
/.vscode/
/z-styling/
/z-ragdocs/
*.iml

# Secrets: ignore every .env variant, keep the checked-in template.
.env
.env.*
!.env.local.example

# Local CF deployment vars (may name internal environments).
vars-*.yml
!vars-dev.yml
!vars-prod.yml

# Service keys / credentials pulled from the platform.
*service-key*
*.pem
*.key
*.p12
*.jks
